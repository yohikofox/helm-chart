apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kamiko.fullname" . }}-test-connection"
  labels:
    {{- include "kamiko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kamiko.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
